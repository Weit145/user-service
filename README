## 👤 User Service

**User Service** — микросервис управления пользователями в составе микросервисной платформы.

Сервис отвечает за хранение и обработку пользовательских данных, управление профилями, а также предоставляет gRPC-интерфейс для взаимодействия с другими микросервисами системы.  
User Service изолирован от внешнего доступа и используется исключительно через API Gateway и внутренние gRPC-вызовы.

---

### 🔧 Зона ответственности

- хранение и управление пользовательскими данными;
- работа с профилями пользователей;
- предоставление gRPC API для внутренних сервисов;
- взаимодействие с собственной базой данных PostgreSQL;
- обеспечение целостности и согласованности данных.

---

### 🚀 Технологический стек

- **Python 3.11+**
- **gRPC** — межсервисное взаимодействие
- **PostgreSQL**
- **asyncpg** — асинхронный драйвер БД
- **SQLAlchemy (async)** — ORM для работы с базой данных
- **Poetry** — управление зависимостями и окружением
- **Docker** — контейнеризация и запуск сервиса

---

### 🧩 Взаимодействие в системе

- принимает gRPC-запросы от:
  - API Gateway;
  - Auth Service;
  - других внутренних микросервисов;
- использует отдельную базу данных `postgres_user`;
- не имеет прямого HTTP-доступа извне.

---
