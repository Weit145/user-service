## 👤 User Service

**User Service** — микросервис управления пользователями в составе микросервисной платформы форума.  
Сервис отвечает за хранение и обработку пользовательских данных, управление профилями, а также предоставляет gRPC-интерфейс для взаимодействия с другими микросервисами системы.  
User Service изолирован от внешнего доступа и используется исключительно через **API Gateway**, внутренние gRPC-вызовы и события **Kafka**.  
Сервис является частью проекта **[Microservices_Site](https://github.com/Weit145/microservices_site)**.


### 🔧 Зона ответственности

- хранение и управление пользовательскими данными;  
- работа с профилями пользователей;    
- публикация и подписка на события через Kafka;  
- взаимодействие с собственной базой данных PostgreSQL (`postgres_user`);  
- обеспечение целостности и согласованности данных.

### 🚀 Технологический стек и зависимости

| Компонент                  | Назначение                                   |
|-----------------------------|----------------------------------------------|
| **Python**                  | Основной язык разработки                     |
| **gRPC / grpcio / grpcio-tools** | Межсервисное взаимодействие             |
| **PostgreSQL**              | Хранилище пользовательских данных           |
| **SQLAlchemy (async)**      | ORM для асинхронной работы с базой данных   |
| **asyncpg**                 | Асинхронный драйвер PostgreSQL               |
| **Alembic**                 | Миграции базы данных                         |
| **dotenv / pydantic-settings** | Работа с настройками и конфигурацией    |
| **aiokafka**                | Публикация/подписка на события Kafka        |
| **Poetry**                  | Управление зависимостями и окружением       |
| **Docker**                  | Контейнеризация и деплой                     |


### 🌐 Взаимодействие в системе

- принимает gRPC-запросы от:  
  - API Gateway;  
  - Post Service;  
- публикует и подписывается на события через Kafka;  
- использует отдельную базу данных `postgres_user`;  
- не имеет прямого HTTP-доступа извне.

### 🛡️ Безопасность

- Вся логика управления пользовательскими данными централизована.  
- Валидация и согласованность данных обеспечиваются на уровне сервиса.  
- Пароли и чувствительные данные обрабатываются безопасно через соответствующие библиотеки (например, Argon2 через `argon2-cffi` и `passlib`).  
- gRPC-контракты описаны в каталоге `proto`.  
