## 👤 User Service

**User Service** — микросервис управления пользователями в составе микросервисной платформы форума.  
Сервис отвечает за хранение и обработку пользовательских данных, управление профилями, а также предоставляет gRPC-интерфейс для взаимодействия с другими микросервисами системы.  
User Service изолирован от внешнего доступа и используется исключительно через **API Gateway**, внутренние gRPC-вызовы и события **Kafka**.  
Сервис является частью проекта **[Microservices_Site](https://github.com/Weit145/microservices_site)**.


### 🔧 Зона ответственности

* **Хранение и управление профилями пользователей:** создание (по событию из `auth-service`), получение, обновление и удаление.
* **Предоставление двух gRPC API:**
  1. **Для API Gateway (`User` service):**
     * `GetUser`: получение данных профиля.
     * `UserUpdate`: обновление профиля.
     * `DeleteUser`: самостоятельное удаление пользователем своего профиля.
  2. **Для внутренних сервисов (`UserPost` service, например, для `post-service`):**
     * `GetByUsername`, `GetByID`, `GetByIDs`: получение информации о пользователях для обогащения данных в других сервисах.
* **Обработка событий жизненного цикла пользователя,** инициированных другими сервисами.

### 🌐 Взаимодействие в системе

* **Предоставляет два gRPC API:**
  * `User` сервис для **API Gateway**.
  * `UserPost` сервис для внутренних клиентов, таких как **Post Service**.

* **Потребляет события из Kafka** для создания и удаления пользователей:
  * `registration`: для создания профиля пользователя после успешной регистрации в `auth-service`.
  * `admin_delete_user`: для удаления пользователя по запросу от `admin-service`.

* **Публикует события в Kafka** при удалении пользователя:
  * `delete`: уведомляет другие сервисы (например, `post-service` и `auth-service`) о необходимости удалить связанные с пользователем данные.

* Использует собственную изолированную базу данных `postgres_user`.
* Не имеет прямого HTTP-доступа извне.

### 🛡️ Безопасность

- Вся логика управления пользовательскими данными централизована.  
- Валидация и согласованность данных обеспечиваются на уровне сервиса.  
- Пароли и чувствительные данные обрабатываются безопасно через соответствующие библиотеки (например, Argon2 через `argon2-cffi` и `passlib`).  
- gRPC-контракты описаны в каталоге `proto`.  
